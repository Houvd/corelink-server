version: '2.1'

services:
  vive_avatar:
    build:
      context: .
      dockerfile: Dockerfile
    image: "vive_avatar:latest"
    restart: unless-stopped
    networks:
      relay_bridge:
        ipv4_address: 10.0.1.3

networks:
  relay_bridge:
    external: true
