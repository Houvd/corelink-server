version: '2.1'

services:
  hanging_holodeck_plugin:
    build:
      context: .
      dockerfile: Dockerfile
#    network_mode: "bridge"
    image: "hanging_holodeck_plugin:latest"
    restart: unless-stopped
    networks:
      relay_bridge:
        ipv4_address: 10.0.1.2

networks:
  relay_bridge:
    external: true

#networks:
#  default:
#    external:
#      name: bridge
