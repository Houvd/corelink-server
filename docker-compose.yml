version: '2.1'

services:
  server:
    build:
      context: .
      dockerfile: Dockerfile
    image: "networktest:latest"
    restart: "no"
    network_mode: "bridge"
    ports:
      - "20000:20000/udp"

networks:
  default:
    external:
      name: bridge
